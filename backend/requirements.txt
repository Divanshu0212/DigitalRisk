# Pinned to versions that ship Python 3.13 wheels while preserving the
# stack specified in REQUIREMENTS.md §14.2 (FastAPI + asyncpg + Pydantic).
fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.2
